---
name: rotechdevelopmentrule
description: Complete projectcontext en richtlijnen voor de Ro-Tech Development workspace (website, lead-finder, master-prompts)
---

# 🚀 RO-TECH DEVELOPMENT - PROJECT MASTER RULE

---

## 🧠 AI GEDRAGSINSTRUCTIES

### Kernprincipes

1. **Lees eerst, handel daarna** - Inspecteer ALTIJD relevante bestanden voordat je code voorstelt. Raad nooit naar bestaande implementaties.

2. **Consistentie boven creativiteit** - Volg bestaande patronen in de codebase. Kijk naar vergelijkbare componenten/functies voor stijl en structuur.

3. **Minimale wijzigingen** - Verander alleen wat nodig is. Geen "verbeteringen" aan omliggende code tenzij expliciet gevraagd.

4. **Nederlandse communicatie** - Praat in het Nederlands met de gebruiker. Code, variabelen en technische termen blijven Engels.

5. **Proactief maar niet opdringerig** - Meld potentiële problemen, maar voer geen ongeplande wijzigingen door.

### Context Herkenning

**Herken het actieve subproject aan:**

| Signaal | Subproject | Actie |
|---------|------------|-------|
| Bestand in `src/`, `public/` | Website | Next.js/TypeScript patronen |
| Bestand in `tools/lead-finder/` | Lead Finder | Python patronen |
| Bestand in `master-prompts/` | Master Prompts | Markdown/template focus |
| Vraag over "leads", "scrapen", "email outreach" | Lead Finder | Python context |
| Vraag over "klantproject", "pakket", "intake" | Master Prompts | Template context |
| Vraag over "pagina", "component", "formulier" | Website | Next.js context |

### Beslisboom: Nieuwe Feature

```
1. Bestaat er al iets vergelijkbaars?
   → JA: Gebruik/extend dat patroon
   → NEE: Ga naar 2

2. Past het in een bestaand bestand?
   → JA: Voeg toe aan bestaand bestand
   → NEE: Maak nieuw bestand in correcte map

3. Is nieuwe dependency nodig?
   → VERMIJD: Gebruik bestaande tools eerst
   → NOODZAKELIJK: Vraag bevestiging aan gebruiker
```

### Beslisboom: Bug Fix

```
1. Reproduceer het probleem mentaal
2. Identificeer root cause (niet symptoom)
3. Zoek vergelijkbare code voor correcte aanpak
4. Fix minimaal - alleen wat kapot is
5. Controleer of fix geen side effects heeft
```

### Wanneer Om Verduidelijking Vragen

- ❓ Meerdere valide interpretaties van de vraag
- ❓ Wijziging zou breaking change zijn
- ❓ Nieuwe externe dependency nodig
- ❓ Significant andere aanpak dan bestaand patroon
- ❓ Onduidelijk welk subproject bedoeld wordt

### Wanneer NIET Vragen (Direct Handelen)

- ✅ Duidelijke bug met voor de hand liggende fix
- ✅ Vraag matcht exact één bestaand patroon
- ✅ Simpele toevoeging/wijziging
- ✅ Expliciet gevraagde actie

---

## 📋 PROJECTOVERZICHT

**Ro-Tech Development (BVR Services)** - Web development agency, Veldhoven, Nederland.

### Subprojecten

| Project | Locatie | Tech | Doel |
|---------|---------|------|------|
| **Website** | `src/`, `public/` | Next.js 15, TypeScript | Bedrijfswebsite |
| **Lead Finder** | `tools/lead-finder/` | Python 3.x | Lead generatie & outreach |
| **Master Prompts** | `master-prompts/` | Markdown | Klantproject templates |

---

## 🛠️ TECH STACK

### Website

| Categorie | Tool | Versie/Details |
|-----------|------|----------------|
| Framework | Next.js | 15+ (App Router) |
| Taal | TypeScript | Strict mode |
| Styling | Tailwind CSS | Utility-first |
| Animaties | Framer Motion | `motion` components |
| Forms | React Hook Form + Zod | Validatie op client & server |
| Email | Resend | Via API routes |
| Icons | Lucide React | `lucide-react` |
| Fonts | Space Grotesk + Inter | Via `next/font/google` |

### Lead Finder

| Categorie | Tool |
|-----------|------|
| Taal | Python 3.x |
| Scraping | Apify API |
| Email | SMTP (Gmail) |
| Output | CSV, JSON |

---

## 📁 BESTANDSSTRUCTUUR (Referentie)

### Belangrijke Bestanden om te Raadplegen

```
WEBSITE PATRONEN:
├── src/app/page.tsx                    # Homepage structuur
├── src/app/diensten/[slug]/page.tsx    # Dynamic route patroon
├── src/app/api/contact/route.ts        # API route patroon
├── src/components/sections/Hero.tsx    # Section component patroon
├── src/components/ui/Button.tsx        # UI component patroon
├── src/components/forms/ContactForm.tsx # Form patroon
├── src/data/services.ts                # Data structuur patroon
├── src/lib/utils.ts                    # Utility functies

LEAD FINDER:
├── tools/lead-finder/apify_lead_finder.py   # Hoofd scraper
├── tools/lead-finder/email_outreach.py      # Email module
├── tools/lead-finder/requirements.txt       # Dependencies

MASTER PROMPTS:
├── master-prompts/README.md                 # Overzicht
├── master-prompts/00-KLANT-INTAKE.md        # Intake template
```

---

## 🎨 DESIGN SYSTEEM

### Kleuren (Exacte Waarden)

```tsx
// Primaire kleuren
const colors = {
  primary: 'bg-indigo-600',      // #4F46E5 - CTA, links
  primaryHover: 'hover:bg-indigo-700',
  secondary: 'bg-violet-600',    // #7C3AED - Accenten
  accent: 'bg-emerald-500',      // #10B981 - Success
  dark: 'bg-slate-900',          // #0F172A - Tekst, dark sections
  light: 'bg-slate-50',          // #F8FAFC - Achtergronden
  muted: 'text-slate-600',       // Body text
};

// Gradient (hero, headers)
<div className="bg-gradient-to-r from-indigo-600 to-violet-600">
```

### Typografie

```tsx
// Headings - Space Grotesk
<h1 className="font-heading text-4xl md:text-5xl lg:text-6xl font-bold tracking-tight">

// Subheadings
<h2 className="font-heading text-3xl md:text-4xl font-bold">

// Body - Inter (default)
<p className="text-base md:text-lg text-slate-600 leading-relaxed">

// Small/Caption
<span className="text-sm text-slate-500">
```

### Spacing & Layout

```tsx
// Section padding
<section className="py-16 md:py-24 lg:py-32">

// Container
<div className="container mx-auto px-4 md:px-6 lg:px-8">

// Grid patronen
<div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 md:gap-8">
```

### Component Voorbeelden

```tsx
// Button - Primary
<button className="inline-flex items-center justify-center bg-indigo-600 hover:bg-indigo-700 text-white px-6 py-3 rounded-lg font-medium transition-colors">

// Button - Secondary/Outline
<button className="inline-flex items-center justify-center border-2 border-indigo-600 text-indigo-600 hover:bg-indigo-50 px-6 py-3 rounded-lg font-medium transition-colors">

// Card
<div className="bg-white rounded-2xl shadow-lg p-6 hover:shadow-xl transition-shadow">

// Card met border
<div className="bg-white rounded-xl border border-slate-200 p-6 hover:border-indigo-300 transition-colors">
```

### Animaties (Framer Motion)

```tsx
// Fade up (meest gebruikt)
<motion.div
  initial={{ opacity: 0, y: 20 }}
  animate={{ opacity: 1, y: 0 }}
  transition={{ duration: 0.5 }}
>

// Stagger container
<motion.div
  initial="hidden"
  animate="visible"
  variants={{
    hidden: {},
    visible: { transition: { staggerChildren: 0.1 } }
  }}
>

// Stagger child
const itemVariants = {
  hidden: { opacity: 0, y: 20 },
  visible: { opacity: 1, y: 0 }
};

// Hover scale
<motion.div whileHover={{ scale: 1.02 }} whileTap={{ scale: 0.98 }}>
```

---

## 🔧 CODE PATRONEN

### Component Template

```tsx
'use client'; // Alleen indien client-side features nodig

import { motion } from 'framer-motion';
import { LucideIcon } from 'lucide-react';

interface ComponentNameProps {
  title: string;
  description?: string;
  icon?: LucideIcon;
}

export function ComponentName({ title, description, icon: Icon }: ComponentNameProps) {
  return (
    <motion.div
      initial={{ opacity: 0, y: 20 }}
      animate={{ opacity: 1, y: 0 }}
      className="bg-white rounded-2xl p-6"
    >
      {Icon && <Icon className="w-6 h-6 text-indigo-600" />}
      <h3 className="font-heading text-xl font-bold mt-4">{title}</h3>
      {description && (
        <p className="text-slate-600 mt-2">{description}</p>
      )}
    </motion.div>
  );
}
```

### Data Bestand Template

```typescript
// src/data/[naam].ts

export interface ItemType {
  id: string;
  slug: string;
  title: string;
  description: string;
  // ... andere velden
}

export const items: ItemType[] = [
  {
    id: '1',
    slug: 'item-slug',
    title: 'Item Titel',
    description: 'Beschrijving',
  },
  // ...
];

// Helper functies
export function getItemBySlug(slug: string): ItemType | undefined {
  return items.find(item => item.slug === slug);
}

export function getAllSlugs(): string[] {
  return items.map(item => item.slug);
}
```

### API Route Template

```typescript
// src/app/api/[route]/route.ts
import { NextRequest, NextResponse } from 'next/server';
import { z } from 'zod';

const RequestSchema = z.object({
  field: z.string().min(1),
  email: z.string().email(),
});

export async function POST(request: NextRequest) {
  try {
    const body = await request.json();
    const validated = RequestSchema.parse(body);
    
    // Verwerk request...
    
    return NextResponse.json({ 
      success: true,
      message: 'Succesvol verwerkt'
    });
    
  } catch (error) {
    if (error instanceof z.ZodError) {
      return NextResponse.json(
        { error: 'Validatie mislukt', details: error.errors },
        { status: 400 }
      );
    }
    
    console.error('API Error:', error);
    return NextResponse.json(
      { error: 'Er ging iets mis' },
      { status: 500 }
    );
  }
}
```

### Python Script Template

```python
#!/usr/bin/env python3
"""
Script beschrijving.

Usage:
    python script_name.py --option value
"""

import argparse
import logging
from pathlib import Path

# Logging setup
logging.basicConfig(
    level=logging.INFO,
    format='%(asctime)s - %(levelname)s - %(message)s'
)
logger = logging.getLogger(__name__)


def main(option: str) -> None:
    """Hoofdfunctie."""
    logger.info(f"Starting with option: {option}")
    
    try:
        # Implementatie...
        pass
    except Exception as e:
        logger.error(f"Error: {e}")
        raise


if __name__ == "__main__":
    parser = argparse.ArgumentParser(description="Script beschrijving")
    parser.add_argument("--option", required=True, help="Optie beschrijving")
    args = parser.parse_args()
    
    main(args.option)
```

---

## ⚠️ ANTI-PATTERNS (Vermijden)

### TypeScript

```typescript
// ❌ NOOIT: any type
const data: any = response;

// ✅ ALTIJD: Specifieke types
interface ApiResponse {
  success: boolean;
  data: DataType;
}
const data: ApiResponse = response;

// ❌ NOOIT: Non-null assertion zonder check
const value = obj!.property;

// ✅ ALTIJD: Proper null handling
const value = obj?.property ?? defaultValue;

// ❌ NOOIT: Enum (performance overhead)
enum Status { Active, Inactive }

// ✅ ALTIJD: Union types of const objects
type Status = 'active' | 'inactive';
const STATUS = { ACTIVE: 'active', INACTIVE: 'inactive' } as const;
```

### React/Next.js

```tsx
// ❌ NOOIT: useEffect voor data die SSR kan
useEffect(() => {
  fetch('/api/data').then(...)
}, []);

// ✅ ALTIJD: Server Components of Server Actions
async function Page() {
  const data = await fetchData();
  return <Component data={data} />;
}

// ❌ NOOIT: Inline styles
<div style={{ marginTop: '20px' }}>

// ✅ ALTIJD: Tailwind classes
<div className="mt-5">

// ❌ NOOIT: Index als key (bij dynamische lijsten)
{items.map((item, index) => <Item key={index} />)}

// ✅ ALTIJD: Unieke identifier
{items.map(item => <Item key={item.id} />)}
```

### Styling

```tsx
// ❌ NOOIT: Hardcoded kleuren buiten design systeem
<div className="bg-[#3B82F6]">

// ✅ ALTIJD: Tailwind presets (indigo-600, etc.)
<div className="bg-indigo-600">

// ❌ NOOIT: Inconsistente spacing
<div className="mt-3 mb-7 px-5">

// ✅ ALTIJD: Consistente spacing schaal (4, 6, 8, 12, 16, 24, 32)
<div className="mt-4 mb-8 px-6">
```

---

## 🚨 KRITIEKE REGELS

### ALTIJD (Verplicht)

| Regel | Reden |
|-------|-------|
| TypeScript strict mode | Voorkomt runtime errors |
| Zod validatie op API input | Security & data integriteit |
| Mobile-first responsive | 60%+ traffic is mobiel |
| Semantic HTML | Accessibility & SEO |
| Error boundaries | Graceful degradation |
| Nederlandse content | Doelgroep is NL |
| Framer Motion voor animaties | Consistentie |

### NOOIT (Verboden)

| Regel | Reden |
|-------|-------|
| `any` type | Verliest TypeScript voordelen |
| Hardcoded secrets | Security risico |
| `console.log` in prod | Performance & professionaliteit |
| Nieuwe packages zonder vraag | Bundle size & maintenance |
| Breaking API changes | Backward compatibility |
| Inline styles | Inconsistentie |

---

## 📞 BEDRIJFSCONTEXT

| Aspect | Details |
|--------|---------|
| **Naam** | Ro-Tech Development |
| **Rechtsvorm** | BVR Services (handelsnaam) |
| **Eigenaar** | Robin |
| **Locatie** | Veldhoven, Noord-Brabant |
| **Doelgroep** | MKB & ZZP in Nederland |
| **USP's** | Persoonlijke aanpak, moderne tech, eerlijke prijzen |
| **Tone of Voice** | Professioneel maar toegankelijk, Brabantse no-nonsense |

### Contact

- **Website:** ro-techdevelopment.com
- **Email:** contact@ro-techdevelopment.com
- **Telefoon:** +31 6 57 23 55 74

### Pakketten (voor Master Prompts context)

| Pakket | Prijs vanaf | Doelgroep |
|--------|-------------|-----------|
| Starter | €997 | ZZP'ers, freelancers |
| Business | €2.497 | MKB, dienstverleners |
| Webshop | €3.997 | Retailers |
| Maatwerk | €7.500 | Complexe processen |

---

## 🔄 SNELLE REFERENTIE

### Commands

```bash
# Website
npm run dev              # Development server
npm run build            # Production build
npm run lint             # Linting

# Lead Finder
cd tools/lead-finder
python apify_quick_start.py                    # Scrape leads
python email_outreach.py --csv output/file.csv # Dry run
python email_outreach.py --csv output/file.csv --live  # Verstuur
```

### Veelgebruikte Imports

```typescript
// Components
import { motion } from 'framer-motion';
import { Icon } from 'lucide-react';
import Link from 'next/link';
import Image from 'next/image';

// Forms
import { useForm } from 'react-hook-form';
import { zodResolver } from '@hookform/resolvers/zod';
import { z } from 'zod';

// Data
import { services, getServiceBySlug } from '@/data/services';
```

---

## 🎯 KWALITEITSCHECKLIST

Voordat je code als "klaar" beschouwt:

- [ ] Types volledig gedefinieerd (geen `any`)
- [ ] Error handling aanwezig
- [ ] Responsive (test mobile breakpoints mentaal)
- [ ] Toegankelijk (aria-labels waar nodig)
- [ ] Consistent met bestaande patronen
- [ ] Geen console.log/debugs
- [ ] Nederlandse teksten correct gespeld

---

*Deze rule maximaliseert AI-prestaties door expliciete gedragsinstructies, decision trees en kwaliteitscriteria te bieden.*
